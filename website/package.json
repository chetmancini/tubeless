{
  "name": "tubeless-website",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node --test scripts/*.test.mjs && node scripts/check-built-links.mjs && node scripts/check-agent-docs.mjs",
    "preview": "astro preview"
  },
  "devDependencies": {
    "@astrojs/markdown-satteri": "0.4.1",
    "astro": "7.3.3",
    "satteri": "0.10.5"
  }
}
