/// <reference types="astro/client" />

// Injected by astro.config.ts (vite.define). The prerender pipeline bundles
// modules away from the source tree, so build-time file reads anchor here
// instead of import.meta.url.
declare const __REPO_ROOT__: string;
