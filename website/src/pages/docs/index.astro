---
import Doc from "../../layouts/Doc.astro";
import { DOC_NAV } from "../../lib/docs";
import { href } from "../../lib/paths";

const human = DOC_NAV.filter((item) =>
  ["getting-started", "recipes", "concepts", "cli", "studio", "comparison"].includes(item.slug)
);
const deep = DOC_NAV.filter((item) =>
  ["child-pipeline-composition", "api-reference"].includes(item.slug)
);
const agents = DOC_NAV.filter((item) =>
  ["agent-guide", "agent-evaluations"].includes(item.slug)
);
---

<Doc title="Developer Documentation" description="Tubeless developer documentation for the TypeScript package, CLI, Local Studio API, and coding agents.">
  <h1>Tubeless developer documentation</h1>
  <p>
    These pages render <code>docs/*.md</code> from the package.
  </p>

  <h2>Usage</h2>
  <div class="agent-grid">
    {human.map((item) => (
      <a href={href(`docs/${item.slug}`)}>
        <h3>{item.label}</h3>
        <p>{item.blurb}</p>
      </a>
    ))}
  </div>

  <h2>Agents</h2>
  <div class="agent-grid">
    <a href={href("agents")}>
      <h3>Entrypoints</h3>
      <p>llms.txt, skill, catalog, and the API report.</p>
    </a>
    {agents.map((item) => (
      <a href={href(`docs/${item.slug}`)}>
        <h3>{item.label}</h3>
        <p>{item.blurb}</p>
      </a>
    ))}
  </div>

  <h2>Reference</h2>
  <div class="agent-grid">
    <a href={href("developers")}>
      <h3>Developer resources</h3>
      <p>Package, CLI, Local Studio API, OpenAPI, and machine-readable entrypoints.</p>
    </a>
    {deep.map((item) => (
      <a href={href(`docs/${item.slug}`)}>
        <h3>{item.label}</h3>
        <p>{item.blurb}</p>
      </a>
    ))}
    <a href={href("api-report.json")}>
      <h3>api-report.json</h3>
      <p>Machine-readable public surface.</p>
    </a>
  </div>
</Doc>
